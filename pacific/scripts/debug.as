#include "query_helpers.as"
#include "log.as"

void _debugAnnounce(Metagame@ metagame, string input) {
	// TODO: comment this line once debugAnnounces not needed
	//sendFactionMessage(metagame, 0, input, 1.0);
}
